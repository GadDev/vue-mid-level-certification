---
"@practice/release": minor
---

Add a "Blog" section to the docs site: nav entry, an auto-generated listing page (`createContentLoader`), a `docs/blog/YYYY-MM-DD-slug.md` post convention with a defined content scope (Vue ecosystem/tooling/AI-tooling commentary, explicitly excluding this repo's own release notes), a blog-scoped sidebar distinct from the existing Guide sidebar, and an inaugural post repurposing `LEARNING_PATH.md`'s per-batch rationale.
