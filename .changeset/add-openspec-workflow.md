---
"@practice/release": patch
---

Add OpenSpec-based change proposal tooling (`.claude/commands/opsx/*`, `.claude/skills/openspec-*`) along with two draft proposals (`add-exercise-gap-agent`, `vue-ecosystem-blog`), and bump the changesets CI workflow's actions to their latest major versions (`checkout@v5`, `pnpm/action-setup@v5`, `setup-node@v5`).
